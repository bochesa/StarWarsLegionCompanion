<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StarWarsLegionMobile.Views.MainPage"
             xmlns:viewmodel="clr-namespace:StarWarsLegionMobile.ViewModels"
             xmlns:model="clr-namespace:StarWarsLegionMobile.Models"
             x:DataType="viewmodel:MainViewModel"
             Title="Home">

    <ScrollView>

        <VerticalStackLayout
            Spacing="25"
            Padding="20,0"
            VerticalOptions="Start">
            

            <Grid RowDefinitions="*,auto">
                <VerticalStackLayout Grid.Row="0" HorizontalOptions="Center">
                    <Label Text="Choose a faction" FontSize="36" />
                    <BoxView HeightRequest="20" Color="Transparent"/>
                <Label Text="Go to Armybuilder menu to create a new army"/>
                </VerticalStackLayout>
            </Grid>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
